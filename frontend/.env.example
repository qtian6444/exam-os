# Supabase (frontend client)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# DeepSeek API key is a SERVER secret, NOT a frontend variable.
# Set it as a Supabase Edge Function secret:
#   supabase secrets set DEEPSEEK_API_KEY=sk-...
# The frontend never reads or sends it.
